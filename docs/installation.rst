Installation
============

Install using pip::

    pip install django-meta

Add ``meta`` and ``sekizai`` to ``INSTALLED_APPS``::

    INSTALLED_APPS = (
        ....
        'meta',
        'sekizai',
    )
